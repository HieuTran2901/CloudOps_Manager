# ============================================================
# CLOUDOPS MANAGER — ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================
# IMPORTANT: DO NOT commit actual secrets or credentials.

# Application Settings
SERVER_PORT=8080
BACKEND_PORT=8080
FRONTEND_PORT=3000
SPRING_PROFILES_ACTIVE=prod

# AWS Target Configuration (Read-Only)
AWS_REGION=us-east-1
AWS_ROLE_ARN=

# Frontend Runtime
VITE_API_BASE_URL=/api/v1/aws
VITE_DEFAULT_REGION=us-east-1