server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful

cloudops:
  aws:
    region: ${AWS_REGION:us-east-1}
    role-arn: ${AWS_ROLE_ARN:}

logging:
  level:
    root: WARN
    com.cloudops.manager: INFO